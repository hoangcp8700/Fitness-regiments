import React from 'react';

interface ProfileMenuProps {}

const ProfileMenu: React.FC<ProfileMenuProps> = () => (
  <div>
    <div className='h-100 w-10 rounded-full bg-red-500' />
  </div>
);

export default ProfileMenu;
